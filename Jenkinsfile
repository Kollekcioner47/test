pipeline {
    agent any
    environment {
        PROJECT_NAME = "Learning of Jenkins"
        MAINTAINER = "Ivan Klimarev"
    }
    stages {
        stage('1-Build') {
            steps {
                echo "Privet ${MAINTAINER}"
                echo "Project is ${PROJECT_NAME}"
            }
        }
     }
}